git config --global user.name "username"

git config --global user.email "emali"

git init ->  for initializing git 

git branch -> list all braches

this line is added from master branch


line 12 is edited in remote repository
